package jacoblazo.foodlogapp;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;

//ENTER LOG ACTIVITY CONTAINS: This is where the user is sent to after they take an image, needs a way to enter information about the image

public class EnterLog extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_enter_log);
    }




    //Take in image from the camera activity, save information as a file on the device's memory.
    //save the image as an image (hidden prefered) and then take the information from the text view's, put them in as text info



}
